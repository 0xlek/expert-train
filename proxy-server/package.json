{
  "name": "proxy-server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "test": "bun test",
    "build": "bun build src/index.ts --compile --outfile=proxy-server"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
